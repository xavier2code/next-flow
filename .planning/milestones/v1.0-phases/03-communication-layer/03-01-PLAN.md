---
phase: 03-communication-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/envelope.py
  - backend/app/schemas/conversation.py
  - backend/app/schemas/agent.py
  - backend/app/schemas/settings.py
  - backend/app/schemas/message.py
  - backend/app/models/message.py
  - backend/app/models/settings.py
  - backend/app/models/__init__.py
  - backend/app/services/conversation_service.py
  - backend/app/services/agent_service.py
  - backend/app/services/settings_service.py
  - backend/app/api/v1/conversations.py
  - backend/app/api/v1/agents.py
  - backend/app/api/v1/settings.py
  - backend/app/api/v1/messages.py
  - backend/app/api/v1/router.py
  - backend/alembic/versions/2026_03_29_add_message_and_user_settings_tables.py
  - backend/tests/unit/test_pagination.py
  - backend/tests/test_conversations.py
  - backend/tests/test_agents.py
  - backend/tests/test_settings.py
  - backend/tests/test_messages.py
autonomous: true
requirements:
  - COMM-01

must_haves:
  truths:
    - "Authenticated user can create, list, get, update, delete, and archive conversations"
    - "Authenticated user can create, list, get, update, and delete agents"
    - "Authenticated user can read and update their own preferences; system config is read-only"
    - "User can POST a message to a conversation and receive 202 Accepted"
    - "All list endpoints return cursor-based paginated responses with envelope format"
    - "Unauthenticated requests to any endpoint return 401"
  artifacts:
    - path: "backend/app/schemas/envelope.py"
      provides: "EnvelopeResponse, PaginatedResponse, PaginationMeta, encode_cursor, decode_cursor"
      exports: ["EnvelopeResponse", "PaginatedResponse", "PaginationMeta", "encode_cursor", "decode_cursor"]
    - path: "backend/app/schemas/conversation.py"
      provides: "ConversationCreate, ConversationUpdate, ConversationResponse"
      exports: ["ConversationCreate", "ConversationUpdate", "ConversationResponse"]
    - path: "backend/app/schemas/agent.py"
      provides: "AgentCreate, AgentUpdate, AgentResponse"
      exports: ["AgentCreate", "AgentUpdate", "AgentResponse"]
    - path: "backend/app/schemas/settings.py"
      provides: "UserSettingsResponse, UserSettingsUpdate, SystemConfigResponse"
      exports: ["UserSettingsResponse", "UserSettingsUpdate", "SystemConfigResponse"]
    - path: "backend/app/schemas/message.py"
      provides: "MessageCreate"
      exports: ["MessageCreate"]
    - path: "backend/app/models/message.py"
      provides: "Message model (id, conversation_id, role, content, metadata_json, created_at)"
      contains: "class Message"
    - path: "backend/app/models/settings.py"
      provides: "UserSettings model (id, user_id, preferences JSON)"
      contains: "class UserSettings"
    - path: "backend/app/services/conversation_service.py"
      provides: "CRUD business logic with cursor pagination"
      exports: ["ConversationService"]
    - path: "backend/app/services/agent_service.py"
      provides: "CRUD business logic"
      exports: ["AgentService"]
    - path: "backend/app/services/settings_service.py"
      provides: "User preferences + system config logic"
      exports: ["SettingsService"]
    - path: "backend/app/api/v1/conversations.py"
      provides: "REST endpoints for conversations CRUD + archive"
      exports: ["router"]
    - path: "backend/app/api/v1/agents.py"
      provides: "REST endpoints for agents CRUD"
      exports: ["router"]
    - path: "backend/app/api/v1/settings.py"
      provides: "REST endpoints for user settings + system config"
      exports: ["router"]
    - path: "backend/app/api/v1/messages.py"
      provides: "POST /conversations/{id}/messages returning 202"
      exports: ["router"]
  key_links:
    - from: "backend/app/api/v1/router.py"
      to: "conversations.py, agents.py, settings.py, messages.py"
      via: "router.include_router()"
      pattern: "include_router.*router"
    - from: "backend/app/api/v1/conversations.py"
      to: "backend/app/services/conversation_service.py"
      via: "ConversationService static methods"
      pattern: "ConversationService\\."
    - from: "backend/app/api/v1/messages.py"
      to: "backend/app/services/conversation_service.py"
      via: "add_message + ownership check"
      pattern: "ConversationService\\.(add_message|get_for_user)"
---

<objective>
Build the REST API layer for conversations, agents, settings, and message posting.

Purpose: External clients need CRUD endpoints to manage their resources (COMM-01). The message POST endpoint (per D-06) accepts chat input and returns 202, deferring the streaming response to the WebSocket channel (built in Plan 02).

Output: 14 new files (6 schemas, 2 models, 3 services, 4 route files) + 1 migration + 5 test files, plus modifications to router.py and models/__init__.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-communication-layer/03-CONTEXT.md
@.planning/phases/03-communication-layer/03-RESEARCH.md
@.planning/phases/03-communication-layer/03-VALIDATION.md
@.planning/phases/03-communication-layer/03-UI-SPEC.md

<interfaces>
<!-- Existing interfaces the executor must use. Read these files for current state. -->

From backend/app/core/exceptions.py:
```python
class AppException(HTTPException): status_code, error_code, message
class NotFoundException(AppException): __init__(message="Resource not found")
class UnauthorizedException(AppException): __init__(message="Not authenticated")
class ForbiddenException(AppException): __init__(message="Forbidden")
class BadRequestException(AppException): __init__(message="Bad request")
class ConflictException(AppException): __init__(message="Conflict")
```

From backend/app/api/deps.py:
```python
async def get_current_user(token, db) -> User
def get_tool_registry(request) -> ToolRegistry
# get_db, get_redis already available
```

From backend/app/db/base.py:
```python
class Base(DeclarativeBase): pass
class TimestampMixin: created_at, updated_at (DateTime timezone-aware, server_default=func.now())
class TenantMixin: tenant_id (UUID, nullable)
```

From backend/app/core/security.py:
```python
def decode_token(token: str) -> dict  # Returns payload with 'sub', 'type', 'exp', 'jti'
```

From backend/app/db/redis.py:
```python
KEY_PREFIX = "nextflow"
def get_redis(request) -> aioredis.Redis
```

From backend/app/core/config.py:
```python
class Settings(BaseSettings):
    database_url, redis_url, jwt_*, default_provider, default_model, openai_api_key, ollama_base_url, debug, log_level, cors_origins
settings = Settings()
```

From backend/app/models/conversation.py:
```python
class Conversation(TimestampMixin, TenantMixin, Base):
    __tablename__ = "conversations"
    id: UUID (pk), user_id: UUID (FK users.id), title: str(255), is_archived: bool
```

From backend/app/models/agent.py:
```python
class Agent(TimestampMixin, TenantMixin, Base):
    __tablename__ = "agents"
    id: UUID (pk), user_id: UUID (FK users.id), name: str(100), system_prompt: Text, model_config: JSON
```

From backend/app/models/user.py:
```python
class User(TimestampMixin, TenantMixin, Base):
    __tablename__ = "users"
    id: UUID (pk), email: str(255 unique), hashed_password: str, display_name: str?, avatar_url: str?, role: str(20)
```

Existing error format (from main.py exception handlers):
```python
{"error": {"code": "ERROR_CODE", "message": "Human-readable description"}}
```

Existing route pattern (from backend/app/api/v1/auth.py):
```python
router = APIRouter(prefix="/auth", tags=["auth"])
# Uses Depends(get_current_user), Depends(get_db), Depends(get_redis)
# Returns Pydantic models directly (e.g., UserResponse.model_validate(user))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas, models, migration, and cursor pagination</name>
  <files>
    backend/app/schemas/envelope.py
    backend/app/schemas/conversation.py
    backend/app/schemas/agent.py
    backend/app/schemas/settings.py
    backend/app/schemas/message.py
    backend/app/models/message.py
    backend/app/models/settings.py
    backend/app/models/__init__.py
    backend/alembic/versions/2026_03_29_add_message_and_user_settings_tables.py
    backend/tests/unit/test_pagination.py
  </files>
  <read_first>
    backend/app/schemas/auth.py
    backend/app/schemas/error.py
    backend/app/models/conversation.py
    backend/app/models/agent.py
    backend/app/models/user.py
    backend/app/db/base.py
    backend/app/core/config.py
    backend/alembic/env.py
    backend/alembic/versions/2026_03_28_1528-1bdd250c71a4_create_initial_tables.py
  </read_first>
  <action>
    **1. Create envelope schemas** in `backend/app/schemas/envelope.py`:
    - `PaginationMeta(BaseModel)`: fields `cursor: str | None = None`, `has_more: bool = False`
    - `EnvelopeResponse(BaseModel, Generic[T])`: field `data: T`, `meta: dict | None = None`
    - `PaginatedResponse(BaseModel, Generic[T])`: fields `data: list[T]`, `meta: PaginationMeta`
    - `encode_cursor(created_at: datetime, item_id: str) -> str`: encode as `base64.urlsafe_b64encode(f"{created_at.isoformat()}|{item_id}".encode()).decode()`
    - `decode_cursor(cursor: str) -> tuple[datetime, str]`: decode by splitting on `|`, parse datetime via `datetime.fromisoformat()`

    **2. Create conversation schemas** in `backend/app/schemas/conversation.py`:
    - `ConversationCreate(BaseModel)`: `title: str = Field(default="New Conversation", max_length=255)`
    - `ConversationUpdate(BaseModel)`: `title: str | None = Field(default=None, max_length=255)`
    - `ConversationResponse(BaseModel)`: fields `id: UUID`, `title: str`, `is_archived: bool`, `created_at: datetime`, `updated_at: datetime`. Set `model_config = {"from_attributes": True}`

    **3. Create agent schemas** in `backend/app/schemas/agent.py`:
    - `AgentCreate(BaseModel)`: `name: str = Field(max_length=100)`, `system_prompt: str | None = None`, `model_config: dict | None = None` (note: field name `model_config` clashes with Pydantic's `model_config` -- use `model_config = {"populate_by_name": True}` on the schema class and alias the field as `config` or use a different field name like `llm_config`). Actually, the Agent model uses `model_config: Mapped[dict | None]` in SQLAlchemy. To avoid the Pydantic name clash, name the Pydantic field `llm_config: dict | None = None` in create/update schemas, and `llm_config: dict | None` in the response. Map it manually in the service layer when creating SQLAlchemy Agent: `Agent(..., model_config=data.llm_config)`.
    - `AgentUpdate(BaseModel)`: `name: str | None = None`, `system_prompt: str | None = None`, `llm_config: dict | None = None`
    - `AgentResponse(BaseModel)`: `id: UUID`, `name: str`, `system_prompt: str | None`, `llm_config: dict | None`, `created_at: datetime`, `updated_at: datetime`. Set `model_config = {"from_attributes": True}`. Use a `@model_validator` to map SQLAlchemy's `model_config` attribute to Pydantic's `llm_config` field.

    **4. Create settings schemas** in `backend/app/schemas/settings.py`:
    - `UserSettingsResponse(BaseModel)`: `preferences: dict`. Set `model_config = {"from_attributes": True}`
    - `UserSettingsUpdate(BaseModel)`: `preferences: dict`
    - `SystemConfigResponse(BaseModel)`: `available_providers: list[str]`, `default_provider: str`, `default_model: str`

    **5. Create message schema** in `backend/app/schemas/message.py`:
    - `MessageCreate(BaseModel)`: `content: str = Field(min_length=1, max_length=10000)`
    - `MessageResponse(BaseModel)`: `id: UUID`, `conversation_id: UUID`, `role: str`, `content: str`, `created_at: datetime`. Set `model_config = {"from_attributes": True}`

    **6. Create Message model** in `backend/app/models/message.py`:
    - `class Message(TimestampMixin, Base)` (no TenantMixin -- messages belong to conversation, not directly to user)
    - `__tablename__ = "messages"`
    - Fields: `id: Mapped[uuid.UUID]` (UUID pk, default=uuid.uuid4), `conversation_id: Mapped[uuid.UUID]` (UUID FK to conversations.id, index, nullable=False), `role: Mapped[str]` (String(20), nullable=False), `content: Mapped[str]` (Text, nullable=False)

    **7. Create UserSettings model** in `backend/app/models/settings.py`:
    - `class UserSettings(TimestampMixin, Base)` (no TenantMixin)
    - `__tablename__ = "user_settings"`
    - Fields: `id: Mapped[uuid.UUID]` (UUID pk, default=uuid.uuid4), `user_id: Mapped[uuid.UUID]` (UUID FK to users.id, unique constraint, nullable=False), `preferences: Mapped[dict]` (JSON, default={})

    **8. Update `backend/app/models/__init__.py`**: Add imports for `Message` and `UserSettings` so Alembic env.py picks them up via `Base.metadata`.

    **9. Create Alembic migration**: Run `cd backend && .venv/bin/alembic revision --autogenerate -m "add message and user_settings tables"`. Verify the generated migration creates both `messages` and `user_settings` tables with correct columns and indexes. Apply with `.venv/bin/alembic upgrade head`.

    **10. Create pagination unit tests** in `backend/tests/unit/test_pagination.py`:
    - Test `encode_cursor` + `decode_cursor` round-trip: create a datetime + UUID, encode, decode, verify they match
    - Test `decode_cursor` with invalid base64 raises exception
    - Test `encode_cursor` produces a valid base64 string
    - Test `PaginationMeta` default values (cursor=None, has_more=False)
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/github/tmp/next-flow/backend && .venv/bin/python -m pytest tests/unit/test_pagination.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/schemas/envelope.py contains "class PaginationMeta" and "class EnvelopeResponse" and "def encode_cursor" and "def decode_cursor"
    - backend/app/schemas/conversation.py contains "class ConversationCreate" and "class ConversationResponse"
    - backend/app/schemas/agent.py contains "class AgentCreate" and "class AgentResponse" and "llm_config"
    - backend/app/schemas/settings.py contains "class UserSettingsResponse" and "class SystemConfigResponse"
    - backend/app/schemas/message.py contains "class MessageCreate" and "class MessageResponse"
    - backend/app/models/message.py contains "class Message" and "__tablename__"
    - backend/app/models/settings.py contains "class UserSettings" and "__tablename__"
    - backend/app/models/__init__.py contains "from app.models.message import Message" and "from app.models.settings import UserSettings"
    - Alembic migration file exists in backend/alembic/versions/ containing "messages" and "user_settings"
    - .venv/bin/python -m pytest tests/unit/test_pagination.py -x exits 0
  </acceptance_criteria>
  <done>
    All Pydantic schemas created with correct field types. Message and UserSettings SQLAlchemy models defined. Alembic migration generated and applied. Pagination encode/decode unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Services, routes, and integration tests</name>
  <files>
    backend/app/services/conversation_service.py
    backend/app/services/agent_service.py
    backend/app/services/settings_service.py
    backend/app/api/v1/conversations.py
    backend/app/api/v1/agents.py
    backend/app/api/v1/settings.py
    backend/app/api/v1/messages.py
    backend/app/api/v1/router.py
    backend/tests/test_conversations.py
    backend/tests/test_agents.py
    backend/tests/test_settings.py
    backend/tests/test_messages.py
  </files>
  <read_first>
    backend/app/services/user_service.py
    backend/app/services/auth_service.py
    backend/app/api/v1/auth.py
    backend/app/api/deps.py
    backend/app/api/v1/router.py
    backend/app/core/exceptions.py
    backend/app/schemas/envelope.py
    backend/app/schemas/conversation.py
    backend/app/schemas/agent.py
    backend/app/schemas/settings.py
    backend/app/schemas/message.py
    backend/app/models/conversation.py
    backend/app/models/agent.py
    backend/app/models/message.py
    backend/app/models/settings.py
    backend/app/main.py
    backend/tests/conftest.py
    backend/tests/test_auth.py
  </read_first>
  <action>
    **1. Create ConversationService** in `backend/app/services/conversation_service.py` (follow user_service.py pattern -- static methods, async, SQLAlchemy select/flush):
    - `create(db, user_id, data) -> Conversation`: create with user_id and title from ConversationCreate
    - `get_for_user(db, user_id, conversation_id) -> Conversation | None`: select where id matches AND user_id matches (ownership check)
    - `list_for_user(db, user_id, cursor_ts, cursor_id, limit=20) -> tuple[list[Conversation], bool]`: cursor-based pagination using `(created_at, id)` composite comparison. Filter by user_id AND is_archived==False. Fetch limit+1 to detect has_more. Return items[:limit], has_more.
    - `update(db, conversation, data) -> Conversation`: update title if provided. Use `for field, value in data` pattern.
    - `delete(db, conversation)`: `await db.delete(conversation)`
    - `archive(db, conversation) -> Conversation`: set `is_archived = True`
    - `add_message(db, conversation_id, role, content) -> Message`: create Message model instance, add to session, flush, return

    **2. Create AgentService** in `backend/app/services/agent_service.py`:
    - `create(db, user_id, data) -> Agent`: create with user_id, name, system_prompt, model_config=data.llm_config
    - `get_for_user(db, user_id, agent_id) -> Agent | None`: ownership check
    - `list_for_user(db, user_id, cursor_ts, cursor_id, limit=20) -> tuple[list[Agent], bool]`: cursor pagination, filter by user_id
    - `update(db, agent, data) -> Agent`: update name, system_prompt, model_config (map data.llm_config to agent.model_config)
    - `delete(db, agent)`: `await db.delete(agent)`

    **3. Create SettingsService** in `backend/app/services/settings_service.py`:
    - `get_or_create(db, user_id) -> UserSettings`: select by user_id, if not found create with default preferences={}
    - `update_settings(db, user_id, data) -> UserSettings`: get_or_create, update preferences, flush, return
    - `get_system_config() -> dict`: return dict with `available_providers=["openai", "ollama"]`, `default_provider=settings.default_provider`, `default_model=settings.default_model`. No DB access -- read from app.core.config.settings.

    **4. Create conversation routes** in `backend/app/api/v1/conversations.py`:
    - `router = APIRouter(prefix="/conversations", tags=["conversations"])`
    - Per D-05: all endpoints return `EnvelopeResponse` or `PaginatedResponse` wrapper
    - `POST ""` (201): create conversation, return `EnvelopeResponse[ConversationResponse]`
    - `GET ""` (200): list with cursor pagination. Query params: `cursor: str | None = None`, `limit: int = Query(default=20, le=100)`. Decode cursor, call service, encode next cursor, return `PaginatedResponse[ConversationResponse]`
    - `GET "/{conversation_id}"` (200): get by id with ownership check, return `EnvelopeResponse[ConversationResponse]`. Raise NotFoundException if not found.
    - `PATCH "/{conversation_id}"` (200): update, return `EnvelopeResponse[ConversationResponse]`
    - `DELETE "/{conversation_id}"` (204): delete, return `Response(status_code=204)`
    - `PATCH "/{conversation_id}/archive"` (200): archive, return `EnvelopeResponse[ConversationResponse]`
    - All endpoints use `Depends(get_current_user)` and `Depends(get_db)`

    **5. Create agent routes** in `backend/app/api/v1/agents.py`:
    - `router = APIRouter(prefix="/agents", tags=["agents"])`
    - `POST ""` (201), `GET ""` (200 paginated), `GET "/{agent_id}"` (200), `PATCH "/{agent_id}"` (200), `DELETE "/{agent_id}"` (204)
    - Same envelope/pagination pattern as conversations

    **6. Create settings routes** in `backend/app/api/v1/settings.py`:
    - `router = APIRouter(prefix="/settings", tags=["settings"])`
    - `GET ""` (200): return `EnvelopeResponse[UserSettingsResponse]` for current user
    - `PATCH ""` (200): update preferences, return `EnvelopeResponse[UserSettingsResponse]`
    - `GET "/system"` (200): return `EnvelopeResponse[SystemConfigResponse]`

    **7. Create messages route** in `backend/app/api/v1/messages.py`:
    - Per D-06: `POST "/conversations/{conversation_id}/messages"` returns 202 Accepted
    - Validate conversation ownership via ConversationService.get_for_user
    - Persist user message via ConversationService.add_message with role="user"
    - Commit the session (so message is persisted before triggering async execution)
    - Trigger agent execution: use `asyncio.create_task` to run graph.astream_events in background, publishing events to Redis pub/sub. For now, create a placeholder `_trigger_agent_execution` function that logs the action (actual execution + Redis pub/sub wiring happens in Plan 02). The function signature: `async def _trigger_agent_execution(conversation_id: str, user_id: str, message: str) -> None` with a `pass` body and a structlog info log.
    - Return `Response(status_code=202)`

    **8. Register all new routers** in `backend/app/api/v1/router.py`:
    - Add imports for conversations, agents, settings, messages routers
    - Add `router.include_router(...)` calls for each

    **9. Create integration tests** in `backend/tests/test_conversations.py`:
    - Follow the test_auth.py pattern: use `async_client` and `registered_user` fixtures
    - Helper: create a function-scoped fixture `auth_headers` that registers a user, logs in, and returns `{"Authorization": f"Bearer {access_token}"}`
    - `test_create_conversation`: POST /api/v1/conversations, assert 201, assert response has data.id, data.title
    - `test_list_conversations_empty`: GET /api/v1/conversations, assert 200, assert data is []
    - `test_list_conversations_with_data`: create 3 conversations, list, assert 3 items, assert meta.has_more == false
    - `test_get_conversation`: create one, GET by id, assert 200 and title matches
    - `test_update_conversation`: create, PATCH title, assert 200 and new title
    - `test_delete_conversation`: create, DELETE, assert 204, GET again assert 404
    - `test_archive_conversation`: create, PATCH /archive, assert is_archived == true
    - `test_conversation_not_found`: GET /conversations/{nonexistent_uuid}, assert 404
    - `test_unauthorized_access`: GET /conversations without auth header, assert 401

    **10. Create integration tests** in `backend/tests/test_agents.py`:
    - Same pattern: auth_headers fixture, CRUD tests
    - `test_create_agent`, `test_list_agents`, `test_get_agent`, `test_update_agent`, `test_delete_agent`
    - `test_agent_with_llm_config`: create agent with llm_config={"provider": "ollama", "model": "llama3"}, verify response has llm_config

    **11. Create integration tests** in `backend/tests/test_settings.py`:
    - `test_get_settings_default`: GET /settings, assert 200, preferences is {} or default
    - `test_update_settings`: PATCH /settings with {"preferences": {"default_model": "gpt-4o"}}, assert 200
    - `test_get_system_config`: GET /settings/system, assert 200, assert available_providers contains "openai"

    **12. Create integration tests** in `backend/tests/test_messages.py`:
    - Create conversation first, then POST message
    - `test_send_message_returns_202`: POST /conversations/{id}/messages with {"content": "hello"}, assert 202
    - `test_send_message_not_found`: POST /conversations/{nonexistent}/messages, assert 404
    - `test_send_message_empty_content`: POST with {"content": ""}, assert 422
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/github/tmp/next-flow/backend && .venv/bin/python -m pytest tests/test_conversations.py tests/test_agents.py tests/test_settings.py tests/test_messages.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/conversation_service.py contains "class ConversationService" with "async def create" and "async def list_for_user" and "async def add_message"
    - backend/app/services/agent_service.py contains "class AgentService" with "async def create" and "async def list_for_user"
    - backend/app/services/settings_service.py contains "class SettingsService" with "async def get_or_create" and "get_system_config"
    - backend/app/api/v1/conversations.py contains 'prefix="/conversations"' and "@router.post" and "@router.get" and "@router.patch" and "@router.delete"
    - backend/app/api/v1/agents.py contains 'prefix="/agents"' and all CRUD endpoints
    - backend/app/api/v1/settings.py contains 'prefix="/settings"' and GET and PATCH endpoints
    - backend/app/api/v1/messages.py contains "status_code=202" and "trigger_agent_execution"
    - backend/app/api/v1/router.py contains "include_router" for conversations, agents, settings, messages
    - All test files exist and tests pass: test_conversations.py, test_agents.py, test_settings.py, test_messages.py
    - .venv/bin/python -m pytest tests/test_conversations.py tests/test_agents.py tests/test_settings.py tests/test_messages.py -x exits 0
  </acceptance_criteria>
  <done>
    Full REST API for CRUD on conversations, agents, and settings with cursor-based pagination and envelope response format. Message POST returns 202. All endpoints require authentication. All integration tests pass.
  </done>
</task>

</tasks>

<verification>
1. Run full test suite: `.venv/bin/python -m pytest tests/ -v --tb=short` -- all tests pass
2. Verify REST endpoint structure matches UI-SPEC.md contract:
   - All endpoints under /api/v1/ prefix
   - Envelope format: `{data: {...}, meta: {cursor, has_more}}`
   - Cursor-based pagination with base64-encoded `created_at|uuid`
   - Error format: `{error: {code, message}}`
3. Verify Alembic migration is applied: `.venv/bin/alembic current` shows latest revision
</verification>

<success_criteria>
- POST /api/v1/conversations returns 201 with ConversationResponse in envelope
- GET /api/v1/conversations returns paginated list with cursor-based pagination
- PATCH /api/v1/conversations/{id}/archive sets is_archived=true
- POST /api/v1/agents returns 201 with AgentResponse in envelope
- GET /api/v1/settings returns user preferences
- GET /api/v1/settings/system returns available providers
- POST /api/v1/conversations/{id}/messages returns 202
- All endpoints return 401 without valid JWT
- All integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-communication-layer/03-01-SUMMARY.md`
</output>
