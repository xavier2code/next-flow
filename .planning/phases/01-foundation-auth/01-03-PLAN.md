---
phase: 01-foundation-auth
plan: 03
type: tdd
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - backend/app/core/security.py
  - backend/app/schemas/__init__.py
  - backend/app/schemas/auth.py
  - backend/app/schemas/user.py
  - backend/app/schemas/error.py
  - backend/app/services/__init__.py
  - backend/app/services/auth_service.py
  - backend/app/services/user_service.py
  - backend/app/api/v1/auth.py
  - backend/app/api/deps.py
  - backend/app/api/v1/router.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_auth.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "User can register with email and password and receive a 201 response with user data"
    - "User can login with valid credentials and receive JWT access + refresh tokens"
    - "User can use access token to access protected endpoints"
    - "User can refresh an expired access token using a valid refresh token"
    - "Refresh token rotation invalidates the old refresh token after use"
    - "Invalid credentials return 401 with consistent error format"
    - "Registration with duplicate email returns 409"
    - "Registration with password shorter than 8 characters returns 422"
  artifacts:
    - path: "backend/app/core/security.py"
      provides: "JWT creation/verification, password hashing"
      exports: ["create_access_token", "create_refresh_token", "decode_token", "hash_password", "verify_password", "get_current_user"]
    - path: "backend/app/services/auth_service.py"
      provides: "Auth business logic (register, login, refresh, logout)"
      exports: ["AuthService"]
    - path: "backend/app/api/v1/auth.py"
      provides: "Auth REST endpoints"
      exports: ["router"]
    - path: "backend/tests/test_auth.py"
      provides: "Test coverage for all auth flows"
      min_lines: 150
  key_links:
    - from: "backend/app/api/v1/auth.py"
      to: "backend/app/services/auth_service.py"
      via: "service layer calls"
      pattern: "auth_service\\."
    - from: "backend/app/services/auth_service.py"
      to: "backend/app/core/security.py"
      via: "token creation and password verification"
      pattern: "security\\."
    - from: "backend/app/services/auth_service.py"
      to: "backend/app/db/redis.py"
      via: "refresh token storage and rotation"
      pattern: "redis.*setex|redis.*get|redis.*delete"
    - from: "backend/app/api/deps.py"
      to: "backend/app/core/security.py"
      via: "get_current_user dependency"
      pattern: "get_current_user"
---

<objective>
Implement the complete JWT authentication system with registration, login, token refresh with rotation, and logout.

Purpose: Delivers AUTH-01 (registration), AUTH-02 (login + JWT), and AUTH-03 (session persistence via refresh). Uses PyJWT + pwdlib[argon2] instead of python-jose + passlib (D-15 override per RESEARCH.md — both original libraries are unmaintained). Refresh tokens are stored in Redis with rotation per D-16.

Output: Working auth endpoints (POST /auth/register, /auth/login, /auth/refresh, /auth/logout per D-18) with comprehensive test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md

@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test scaffold, schemas, security module, and auth service (RED phase)</name>
  <files>
    backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_auth.py,
    backend/app/schemas/__init__.py, backend/app/schemas/auth.py, backend/app/schemas/user.py,
    backend/app/schemas/error.py, backend/app/core/security.py,
    backend/app/services/__init__.py, backend/app/services/auth_service.py, backend/app/services/user_service.py
  </files>
  <read_first>
    - backend/app/core/config.py (settings for JWT secret, algorithm, expiry times)
    - backend/app/core/exceptions.py (AppException, ConflictException, UnauthorizedException, BadRequestException)
    - backend/app/db/session.py (get_db dependency)
    - backend/app/db/redis.py (get_redis dependency, KEY_PREFIX)
    - backend/app/models/user.py (User model with email, hashed_password, etc.)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md (D-13 through D-18 decisions)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (Pattern 3: JWT with PyJWT, Pattern 5: Redis refresh tokens, Pitfall 2: refresh race condition, Pitfall 6: timing attack protection)
  </read_first>
  <behavior>
    - test_register_success: POST /auth/register with valid email+password returns 201 and user data (id, email, display_name)
    - test_register_duplicate_email: POST /auth/register with existing email returns 409 ConflictException
    - test_register_short_password: POST /auth/register with 7-char password returns 422 validation error
    - test_register_missing_email: POST /auth/register without email returns 422 validation error
    - test_login_success: POST /auth/login with valid credentials returns 200 with access_token, refresh_token, token_type="bearer"
    - test_login_wrong_password: POST /auth/login with wrong password returns 401
    - test_login_nonexistent_user: POST /auth/login with unknown email returns 401
    - test_refresh_token_success: POST /auth/refresh with valid refresh_token returns 200 with new access_token and new refresh_token
    - test_refresh_token_rotation: Using the same refresh token twice returns 401 on second attempt (old token invalidated)
    - test_refresh_expired_token: POST /auth/refresh with expired token returns 401
    - test_logout_success: POST /auth/logout with valid token returns 200
    - test_access_protected_endpoint: GET /auth/me with valid access token returns user data
    - test_access_protected_no_token: GET /auth/me without token returns 401
  </behavior>
  <action>
    **RED phase: Write all tests first, then create minimal stubs to make imports work.**

    **1. Create `backend/tests/__init__.py`** (empty)

    **2. Create `backend/tests/conftest.py`:**
    - Import pytest, httpx, asyncio
    - Import `app` from `app.main`
    - Import `async_session_factory`, `engine` from `app.db.session`
    - Import `Base` from `app.models`
    - Import `get_db` from `app.api.deps`
    - Import `settings` from `app.core.config`
    - Create `@pytest.fixture(scope="session")` event_loop (use asyncio.new_event_loop)
    - Create `@pytest.fixture(scope="session")` test database:
      - Create a test engine with `settings.database_url` (but add `_test` suffix to DB name or use same DB)
      - Run `Base.metadata.create_all(engine)` to create all tables
      - Yield
      - Run `Base.metadata.drop_all(engine)` to clean up
    - Create `@pytest.fixture` async_client:
      - Override `get_db` to use test session
      - Create `httpx.AsyncClient(app=app, base_url="http://test")`
      - Yield client
      - Close client
    - Create `@pytest.fixture` test_redis:
      - Import `redis.asyncio as aioredis`
      - Create test Redis client (use db=1 to avoid conflict with dev data)
      - Override the app's Redis with test Redis
      - Yield
      - Close test Redis and flush db

    **3. Create `backend/tests/test_auth.py`** with all test cases from `<behavior>`:
    - Each test uses `async_client` and `test_redis` fixtures
    - Use pytest-asyncio (asyncio_mode = "auto" in pyproject.toml)
    - All assertions use assert statements, not unittest

    **4. Create `backend/app/schemas/error.py`:**
    ```python
    from pydantic import BaseModel

    class ErrorDetail(BaseModel):
        code: str
        message: str

    class ErrorResponse(BaseModel):
        error: ErrorDetail
    ```

    **5. Create `backend/app/schemas/auth.py`:**
    ```python
    from pydantic import BaseModel, EmailStr, Field

    class RegisterRequest(BaseModel):
        email: EmailStr
        password: str = Field(min_length=8, max_length=128)
        display_name: str | None = None

    class LoginRequest(BaseModel):
        email: EmailStr
        password: str

    class TokenResponse(BaseModel):
        access_token: str
        refresh_token: str
        token_type: str = "bearer"

    class RefreshRequest(BaseModel):
        refresh_token: str

    class LogoutRequest(BaseModel):
        refresh_token: str | None = None
    ```
    Note: Add `email-validator` to pyproject.toml dependencies for EmailStr validation.

    **6. Create `backend/app/schemas/user.py`:**
    ```python
    import uuid
    from datetime import datetime
    from pydantic import BaseModel, EmailStr

    class UserResponse(BaseModel):
        id: uuid.UUID
        email: EmailStr
        display_name: str | None
        avatar_url: str | None
        role: str
        created_at: datetime

        model_config = {"from_attributes": True}
    ```

    **7. Create `backend/app/schemas/__init__.py`** (re-export key schemas)

    **8. Create `backend/app/core/security.py`** per Pattern 3 (PyJWT):
    - Import `jwt` from `PyJWT`, `InvalidTokenError`
    - Import `PasswordHash` from `pwdlib`
    - Import `settings` from `app.core.config`
    - Import `OAuth2PasswordBearer` from `fastapi.security`
    - Define `ALGORITHM = settings.jwt_algorithm`
    - Create `password_hash = PasswordHash.recommended()`
    - Create `DUMMY_HASH = password_hash.hash("dummypasswordfortimingattackprotection")` (Pitfall 6)
    - `hash_password(password: str) -> str`: return `password_hash.hash(password)`
    - `verify_password(plain: str, hashed: str) -> bool`: return `password_hash.verify(plain, hashed)`
    - `create_access_token(subject: str) -> str`: encode JWT with `sub=subject`, `type="access"`, exp = now + `settings.access_token_expire_minutes` minutes
    - `create_refresh_token(subject: str) -> str`: encode JWT with `sub=subject`, `type="refresh"`, exp = now + `settings.refresh_token_expire_days` days
    - `decode_token(token: str) -> dict`: decode and return payload, raise `UnauthorizedException` on `InvalidTokenError`
    - `oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")`

    **9. Create `backend/app/services/user_service.py`:**
    - `get_by_email(db: AsyncSession, email: str) -> User | None`: query User table by email
    - `get_by_id(db: AsyncSession, user_id: str) -> User | None`: query User table by id
    - `create_user(db: AsyncSession, email: str, hashed_password: str, display_name: str | None) -> User`: insert new User

    **10. Create `backend/app/services/auth_service.py`:**
    - Import security functions, user_service, settings
    - Import Redis type from `redis.asyncio`
    - `register(db, data: RegisterRequest) -> User`:
      - Check if email exists: if yes, raise ConflictException("EMAIL_EXISTS")
      - Hash password
      - Create user via user_service
      - Return user
    - `login(db, redis, data: LoginRequest) -> TokenResponse`:
      - Get user by email (use DUMMY_HASH for timing attack protection — Pitfall 6)
      - Verify password
      - Create access + refresh tokens
      - Store refresh token in Redis: key=`nextflow:refresh_token:{user_id}:{jti}`, value=token, TTL=refresh_token_expire_days in seconds
      - Add `jti` (JWT ID) claim to refresh token for unique identification
      - Return TokenResponse
    - `refresh(db, redis, data: RefreshRequest) -> TokenResponse`:
      - Decode refresh token, verify `type == "refresh"`
      - Validate stored token exists in Redis
      - Delete old refresh token from Redis (rotation per D-16)
      - Create new access + refresh tokens
      - Store new refresh token in Redis
      - Return TokenResponse
    - `logout(redis, user_id: str, refresh_token: str | None)`:
      - If refresh_token provided: decode, validate, delete from Redis
      - Return success

    **11. Create `backend/app/services/__init__.py`** (empty)

    Run tests — they should FAIL (RED phase). Commit with message: `test(01-03): add auth test suite for AUTH-01, AUTH-02, AUTH-03`
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/github/tmp/next-flow/backend && python -c "from app.schemas.auth import RegisterRequest, LoginRequest, TokenResponse, RefreshRequest; from app.core.security import hash_password, create_access_token, create_refresh_token; from app.services.auth_service import AuthService; print('PASS: All auth modules import successfully')" 2>&1</automated>
  </verify>
  <done>
    - All Pydantic schemas defined (RegisterRequest, LoginRequest, TokenResponse, RefreshRequest, UserResponse, ErrorResponse)
    - Security module with PyJWT token creation/verification and pwdlib password hashing
    - AuthService with register, login, refresh, logout methods
    - UserService with get_by_email, get_by_id, create_user methods
    - Test file with 13 test cases covering all auth flows
    - Tests FAIL because auth routes don't exist yet (RED phase)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create auth routes, wire dependencies, and pass all tests (GREEN phase)</name>
  <files>
    backend/app/api/v1/auth.py, backend/app/api/v1/router.py, backend/app/api/deps.py,
    backend/tests/conftest.py, backend/pyproject.toml
  </files>
  <read_first>
    - backend/app/api/v1/health.py (existing router pattern to follow)
    - backend/app/api/v1/router.py (need to add auth router)
    - backend/app/api/deps.py (need to add get_current_user dependency)
    - backend/app/services/auth_service.py (service layer to wire)
    - backend/app/core/security.py (oauth2_scheme and get_current_user)
    - backend/app/db/redis.py (get_redis dependency)
    - backend/tests/test_auth.py (all tests to pass)
  </read_first>
  <behavior>
    All 13 tests from Task 1 must pass:
    - test_register_success -> 201
    - test_register_duplicate_email -> 409
    - test_register_short_password -> 422
    - test_register_missing_email -> 422
    - test_login_success -> 200 + tokens
    - test_login_wrong_password -> 401
    - test_login_nonexistent_user -> 401
    - test_refresh_token_success -> 200 + new tokens
    - test_refresh_token_rotation -> 401 on reuse
    - test_refresh_expired_token -> 401
    - test_logout_success -> 200
    - test_access_protected_endpoint -> 200 + user data
    - test_access_protected_no_token -> 401
  </behavior>
  <action>
    **GREEN phase: Implement the auth routes and wire everything to pass all tests.**

    **1. Update `backend/app/api/deps.py`:**
    - Keep existing get_db and get_redis re-exports
    - Add `get_current_user` dependency:
      ```python
      from fastapi import Depends, HTTPException
      from fastapi.security import OAuth2PasswordBearer
      from sqlalchemy.ext.asyncio import AsyncSession
      from app.core.security import oauth2_scheme, decode_token
      from app.services.user_service import get_by_id
      from app.api.deps import get_db

      async def get_current_user(
          token: str = Depends(oauth2_scheme),
          db: AsyncSession = Depends(get_db),
      ):
          payload = decode_token(token)
          if payload.get("type") != "access":
              raise HTTPException(status_code=401, detail="Invalid token type")
          user_id = payload.get("sub")
          if user_id is None:
              raise HTTPException(status_code=401, detail="Invalid token")
          user = await get_by_id(db, user_id)
          if user is None:
              raise HTTPException(status_code=401, detail="User not found")
          return user
      ```

    **2. Create `backend/app/api/v1/auth.py`** per D-18:
    - Define `router = APIRouter(prefix="/auth", tags=["auth"])`
    - Import schemas, services, dependencies
    - `POST /register`:
      - Accept `RegisterRequest` body
      - Call `auth_service.register(db, data)`
      - Return 201 with `UserResponse.model_validate(user)`
    - `POST /login`:
      - Accept `LoginRequest` body
      - Get Redis from `Depends(get_redis)`
      - Call `auth_service.login(db, redis, data)`
      - Return 200 with `TokenResponse`
    - `POST /refresh`:
      - Accept `RefreshRequest` body
      - Get Redis from `Depends(get_redis)`
      - Call `auth_service.refresh(db, redis, data)`
      - Return 200 with `TokenResponse`
    - `POST /logout`:
      - Accept `LogoutRequest` body (optional refresh_token)
      - Get current user from `Depends(get_current_user)`
      - Get Redis from `Depends(get_redis)`
      - Call `auth_service.logout(redis, str(user.id), data.refresh_token)`
      - Return 200 with `{"message": "Logged out successfully"}`
    - `GET /me`:
      - Require `Depends(get_current_user)`
      - Return 200 with `UserResponse.model_validate(user)`

    **3. Update `backend/app/api/v1/router.py`:**
    - Add import of auth router
    - Include auth router: `router.include_router(auth_router)`

    **4. Add `email-validator` to `backend/pyproject.toml` dependencies** (required for Pydantic EmailStr).

    **5. Update `backend/tests/conftest.py`** if needed:
    - Ensure test fixtures correctly override dependencies
    - Make sure test database uses separate test schema or test DB to avoid polluting dev data
    - Add fixture for creating a test user (used by login/refresh/logout tests)

    **6. Run all tests and verify they pass:**
    - `cd /Users/xavier/Projects/github/tmp/next-flow/backend && python -m pytest tests/test_auth.py -v`
    - All 13 tests must pass

    **7. Run full test suite:**
    - `cd /Users/xavier/Projects/github/tmp/next-flow/backend && python -m pytest tests/ -v`
    - All tests pass (including any existing health check tests)

    Commit with message: `feat(01-03): implement JWT auth with registration, login, refresh, logout (AUTH-01, AUTH-02, AUTH-03)`
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/github/tmp/next-flow/backend && python -m pytest tests/test_auth.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - POST /api/v1/auth/register returns 201 for new user, 409 for duplicate email, 422 for short password
    - POST /api/v1/auth/login returns 200 with JWT tokens for valid credentials, 401 for invalid
    - POST /api/v1/auth/refresh returns new token pair, old refresh token invalidated (rotation)
    - POST /api/v1/auth/logout returns 200
    - GET /api/v1/auth/me returns user data with valid token, 401 without token
    - All 13 test cases pass
    - Timing attack protection on login endpoint (DUMMY_HASH for non-existent users)
    - Refresh tokens stored in Redis with TTL and rotation
  </done>
</task>

</tasks>

<verification>
1. `cd backend && python -m pytest tests/test_auth.py -v` — all 13 tests pass
2. `cd backend && python -m pytest tests/ -v` — full suite green
3. Manual API test: register user, login, use token on /auth/me, refresh token, logout
4. Verify refresh token rotation: use refresh token twice — second attempt fails
5. Verify error format consistency: all errors return `{"error": {"code": "...", "message": "..."}}`
</verification>

<success_criteria>
- Registration creates user with hashed password (AUTH-01)
- Login returns JWT access + refresh tokens (AUTH-02)
- Refresh token rotation works — old tokens invalidated (AUTH-03, D-16)
- Protected endpoints require valid access token (AUTH-02)
- All 13 auth tests pass
- No security issues: timing attack protection, no token in localStorage, refresh rotation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
