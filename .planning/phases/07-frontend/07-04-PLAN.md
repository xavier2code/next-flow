---
phase: 07-frontend
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - frontend/src/App.tsx
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - UI-10

must_haves:
  truths:
    - "Full application renders without errors at all routes"
    - "Login and register flows work end-to-end"
    - "Chat interface sends messages and displays streaming responses"
    - "Management pages render with list+detail layout"
    - "Settings page shows theme toggle"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Final route wiring with all pages"
      contains: "ChatPage"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "all page components"
      via: "react-router Routes"
      pattern: "Route.*element"
---

<objective>
Final integration checkpoint: verify the complete frontend application works end-to-end by running it and manually testing all user flows.

Purpose: Catch integration issues that automated tests cannot catch (layout rendering, visual correctness, API connectivity, WebSocket streaming). This is the user acceptance test.

Output: Verified, working frontend application.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-frontend/07-01-SUMMARY.md
@.planning/phases/07-frontend/07-02-SUMMARY.md
@.planning/phases/07-frontend/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build verification and cleanup</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    @frontend/src/App.tsx (current routing state)
    @frontend/src/main.tsx (app entry point)
  </read_first>
  <action>
    1. Run TypeScript type check: `cd frontend && npx tsc --noEmit` -- fix any errors
    2. Run Vitest: `cd frontend && npx vitest run` -- fix any failing tests
    3. Run production build: `cd frontend && npm run build` -- fix any build errors
    4. Verify no console warnings or unused imports in key files
    5. Ensure all routes in App.tsx map to real components (no placeholder divs remaining)
    6. Verify all Chinese text is in place (no English-only UI text)
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/github/tmp/next-flow/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `cd frontend && npm run build` succeeds with zero errors
    - `cd frontend && npx tsc --noEmit` passes
    - `cd frontend && npx vitest run` passes
    - No placeholder divs remain in App.tsx routes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User acceptance testing of complete frontend</name>
  <what-built>Complete NextFlow frontend SPA with all 10 UI requirements implemented</what-built>
  <how-to-verify>
    **Prerequisites:** Backend running on localhost:8000 (`cd backend && uvicorn app.main:app --reload`)

    **Start frontend:** `cd frontend && npm run dev` (opens at http://localhost:5173)

    **Test 1 - Auth (UI-01, UI-02):**
    1. Visit http://localhost:5173 -- should redirect to /login
    2. Click "没有账号？注册" -- should show register page with "注册 NextFlow" heading
    3. Fill email + password (8+ chars) + optional display name, click "注册"
    4. Should redirect to main app (chat view)
    5. Refresh page -- should stay authenticated (token refresh)

    **Test 2 - Chat (UI-03, UI-04, UI-08):**
    6. Click "新建对话" in sidebar -- new conversation created
    7. See welcome screen: "你好！我是 NextFlow 助手" with example prompts
    8. Type message in input box, press Enter -- message appears as right-aligned bubble
    9. AI response streams in as left-aligned bubble with Markdown rendering
    10. Press Shift+Enter in input -- creates newline (does not send)
    11. Click different conversation in sidebar -- switches context

    **Test 3 - Streaming events (UI-05, UI-06):**
    12. Send a message that triggers tool use (if agent configured with tools)
    13. Side panel should auto-open on the right (320px)
    14. Thinking entries show as collapsible "思考过程" items
    15. Tool calls show as cards with Wrench icon and "调用" badge
    16. Tool results show as cards with "结果" badge
    17. Connection status dot visible in chat header

    **Test 4 - Agent management (UI-07):**
    18. Click Management icon (LayoutGrid) in activity bar
    19. See "智能体" tab selected, agent list on left
    20. Click "创建智能体" -- empty form appears on right
    21. Fill name, select model, enter system prompt, adjust temperature slider
    22. Click "保存" -- agent saved and appears in list

    **Test 5 - Skill management (UI-09):**
    23. Click "技能" tab
    24. Click "上传技能" -- file dialog opens, filtered to .zip
    25. Upload a skill ZIP -- skill appears in list with "disabled" status badge
    26. Click skill -- detail shows on right with enable/disable Switch toggle
    27. Toggle Switch to enable -- status changes

    **Test 6 - MCP management (UI-10):**
    28. Click "MCP 服务器" tab
    29. Click "注册服务器" -- registration form appears
    30. Fill name + URL, select transport type, click "注册"
    31. Server appears in list with status dot (color indicates connection state)
    32. Click server -- detail shows config + discovered tools list

    **Test 7 - Settings (UI-01 D-20):**
    33. Click Settings icon (gear) in activity bar
    34. See "外观" section with dark mode Switch
    35. Toggle dark mode off -- theme switches to light
    36. See account info (email, name)
    37. Click "退出登录" -- redirected to login page

    **Visual checks:**
    - All text in Chinese (no English UI labels)
    - Dark theme is default
    - Activity bar 48px with 3 icons
    - Sidebar 260px
    - Layout does not break at 1280px+
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` succeeds
2. `cd frontend && npx vitest run` passes
3. Manual UAT checklist passes all 37 test steps
</verification>

<success_criteria>
- Production build succeeds without errors
- All manual verification steps pass
- No visual regressions or layout breaks
- All UI text in Chinese
- All 10 UI requirements verified working
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend/07-04-SUMMARY.md`
</output>
