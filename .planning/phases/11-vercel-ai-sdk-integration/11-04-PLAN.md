---
phase: 11-vercel-ai-sdk-integration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/chat.py
  - frontend/src/components/chat/SidePanel.tsx
  - frontend/src/components/chat/ChatView.tsx
  - frontend/src/components/chat/ReasoningEntry.tsx
autonomous: true
gap_closure: true
requirements: [SC-05]

must_haves:
  truths:
    - "Reasoning content from reasoning-delta events is visible in SidePanel"
    - "Regenerate button appears in chat header and calls reload()"
    - "Backend emits reasoning-end event to close reasoning parts"
  artifacts:
    - path: "frontend/src/components/chat/ReasoningEntry.tsx"
      provides: "Collapsible reasoning display component"
      min_lines: 20
    - path: "frontend/src/components/chat/SidePanel.tsx"
      provides: "Side panel rendering both reasoning and tool invocation entries"
      contains: "reasoningEntries"
  key_links:
    - from: "backend/app/api/v1/chat.py"
      to: "reasoning-end event"
      via: "SSE event emission after reasoning-delta sequence ends"
      pattern: "reasoning-end"
    - from: "frontend/src/components/chat/SidePanel.tsx"
      to: "UIMessage.parts"
      via: "Filter for reasoning parts from message.parts array"
      pattern: "parts.*reasoning"
    - from: "frontend/src/components/chat/ChatView.tsx"
      to: "reload()"
      via: "Regenerate button onClick handler in header"
      pattern: "onClick.*reload"
---

<objective>
Fix reasoning display pipeline and add Regenerate button to close the two most impactful verification gaps from Phase 11.

Purpose: The backend correctly emits reasoning-start and reasoning-delta SSE events, but never emits reasoning-end, which prevents useChat from finalizing reasoning parts in UIMessage. Additionally, the SidePanel only renders toolInvocations and ignores reasoning parts. Finally, reload() is destructured from useChat but not wired to any UI element.

This plan:
1. Adds reasoning-end emission to the backend SSE stream (after the last reasoning-delta and before text-start or finish)
2. Creates a ReasoningEntry component for the SidePanel
3. Updates SidePanel to extract and render reasoning parts from UIMessage.parts
4. Wires a Regenerate button to reload() in the ChatView header

Output: Working reasoning display in SidePanel, Regenerate button in chat header
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-vercel-ai-sdk-integration/11-VERIFICATION.md
@.planning/phases/11-vercel-ai-sdk-integration/11-02-SUMMARY.md

<interfaces>
From ai package (node_modules/ai/dist/index.d.ts):
```typescript
type ReasoningUIPart = {
    type: 'reasoning';
    text: string;
    state?: 'streaming' | 'done';
    providerMetadata?: ProviderMetadata;
};

interface UIMessage<METADATA, DATA_PARTS, TOOLS> {
    id: string;
    role: 'system' | 'user' | 'assistant' | 'data';
    content: string;
    parts: Array<UIMessagePart<DATA_PARTS, TOOLS>>;
    toolInvocations?: ToolInvocation[];
    // ...
}
```

The UIMessage.parts array includes ReasoningUIPart entries. Use `isReasoningUIPart()` from 'ai' to filter, or check `part.type === 'reasoning'`.

From backend/app/api/v1/chat.py (current):
- Lines 92-121: Processes ThinkTagFilter output, emits reasoning-start + reasoning-delta for thinking type
- Lines 148-178: Flushes remaining ThinkTagFilter buffer at chain_end
- Lines 184-204: Handles on_custom_event for thinking content
- Missing: No reasoning-end event is ever emitted

From frontend/src/components/chat/SidePanel.tsx (current):
- Lines 25-33: Only extracts toolInvocations from messages
- No reasoning extraction from message.parts

From frontend/src/components/chat/ChatView.tsx (current):
- Line 43: `reload` destructured from useChat but never used in JSX
- Lines 127-149: Header area where Regenerate button should go
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix backend reasoning-end emission and add Regenerate button + reasoning display</name>
  <files>
    backend/app/api/v1/chat.py
    frontend/src/components/chat/ReasoningEntry.tsx
    frontend/src/components/chat/SidePanel.tsx
    frontend/src/components/chat/ChatView.tsx
  </files>
  <read_first>
    - backend/app/api/v1/chat.py (full file, understand reasoning event emission flow)
    - frontend/src/components/chat/SidePanel.tsx (current tool invocation rendering)
    - frontend/src/components/chat/ChatView.tsx (current header and useChat config)
    - frontend/src/components/chat/ToolCallCard.tsx (reference for card styling pattern)
    - frontend/src/components/chat/ToolResultCard.tsx (reference for card styling pattern)
  </read_first>
  <acceptance_criteria>
    - Backend emits reasoning-end SSE event after the last reasoning-delta in each reasoning sequence
    - SidePanel extracts reasoning parts from UIMessage.parts and renders them via ReasoningEntry
    - ChatView has a Regenerate button visible when status is not streaming/submitted
    - Regenerate button calls reload() on click
    - TypeScript compiles without errors
  </acceptance_criteria>
  <action>
    **Part A: Backend reasoning-end emission (backend/app/api/v1/chat.py)**

    The backend emits reasoning-start and reasoning-delta but never reasoning-end. The AI SDK useChat hook needs reasoning-end to finalize the ReasoningUIPart state to 'done'.

    Add `reasoning-end` emission at three transition points where reasoning ends and text begins (or the stream finishes):

    1. In the ThinkTagFilter processing loop (around line 98-121): When `ftype` transitions from `"thinking"` to `"chunk"`, emit a `reasoning-end` event BEFORE emitting the `text-start` event. Track `has_reasoning_start` state -- if it is true when a chunk arrives, emit reasoning-end first, set `has_reasoning_start = false`.

    2. In the ThinkTagFilter flush (around line 148-178): Same transition logic -- if flushing transitions from thinking to chunk, emit reasoning-end.

    3. In the finish event emission (around line 181): If `has_reasoning_start` is still true when finish is emitted (reasoning was the last thing emitted), emit reasoning-end before finish.

    The reasoning-end event format per AI SDK protocol:
    ```python
    yield _sse_line({
        "type": "reasoning-end",
        "id": reason_id,
    })
    ```

    **Part B: ReasoningEntry component (frontend/src/components/chat/ReasoningEntry.tsx)**

    Create a new component following the same styling pattern as ToolCallCard/ToolResultCard:
    - Use `Card` from `@/components/ui/card`, `Badge` from `@/components/ui/badge`
    - Use `Brain` icon from `lucide-react` (reasoning/thinking icon)
    - Props: `{ text: string; state?: 'streaming' | 'done' }`
    - Show a "思考" badge (Chinese for "Thinking")
    - Display reasoning text in a `<p>` tag with `text-sm leading-relaxed text-muted-foreground`
    - When `state === 'streaming'`, show a subtle pulse animation on the text or a "思考中..." indicator
    - Make the card collapsible (use `Collapsible` from `@/components/ui/collapsible`), default open for streaming, collapsed for done state
    - Reasoning text may be multi-line -- use `whitespace-pre-wrap` for formatting

    **Part C: SidePanel reasoning extraction (frontend/src/components/chat/SidePanel.tsx)**

    Update the SidePanel to extract reasoning parts alongside tool invocations:

    1. Add import for `isReasoningUIPart` from `'ai'` (the base package, not @ai-sdk/react)
    2. Create a new `reasoningEntries` array by flatMapping over messages and filtering `message.parts`:
       ```typescript
       const reasoningEntries = messages.flatMap((msg) =>
         (msg.parts ?? []).filter((part): part is ReasoningUIPart => part.type === 'reasoning')
       )
       ```
       Import `ReasoningUIPart` type from `'ai'`.
    3. In the entries list rendering, render reasoning entries BEFORE tool entries (reasoning happens before tools in the flow):
       - Map each reasoning entry to `<ReasoningEntry key={...} text={entry.text} state={entry.state} />`
       - Use the entry's index combined with message id as key
    4. Show "等待执行事件" placeholder only when BOTH reasoningEntries and allEntries are empty

    **Part D: Regenerate button (frontend/src/components/chat/ChatView.tsx)**

    Add a Regenerate button to the header area:

    1. In the header `<div>` (lines 134-149), add a Regenerate button that:
       - Is visible when `status !== 'streaming'` AND `status !== 'submitted'` AND `messages.length > 0`
       - Calls `reload()` on click
       - Uses `RefreshCw` icon from `lucide-react`
       - Has the same styling as the Stop button: `text-xs text-muted-foreground hover:text-foreground`
       - Shows the text "重新生成" (Chinese for "Regenerate")
    2. Import `RefreshCw` from `lucide-react`
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/github/next-flow/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Backend emits reasoning-end event at all transition points (reasoning-to-text, reasoning-to-finish)
    - SidePanel renders reasoning content from UIMessage.parts using ReasoningEntry component
    - ChatView has a visible Regenerate button that calls reload()
    - TypeScript compiles with zero errors
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `cd frontend && npx tsc --noEmit`
2. Backend reasoning-end emission: grep confirms "reasoning-end" in chat.py at all transition points
3. ReasoningEntry component exists with correct props interface
4. SidePanel imports and uses ReasoningEntry, extracts parts from UIMessage
5. ChatView has Regenerate button calling reload()
</verification>

<success_criteria>
- `reasoning-end` SSE events are emitted by backend at all reasoning-to-text and reasoning-to-finish transitions
- SidePanel shows reasoning content in a collapsible card when the LLM produces thinking/reasoning content
- Regenerate button is visible in chat header after assistant response completes
- All changes compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-vercel-ai-sdk-integration/11-04-SUMMARY.md`
</output>
